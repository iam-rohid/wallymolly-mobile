import React from "react";
import { StyleSheet, Text, View } from "react-native";

const AuthRegisterScreen = () => {
  return (
    <View>
      <Text>Auth Register Screen</Text>
    </View>
  );
};

export default AuthRegisterScreen;

const styles = StyleSheet.create({});
